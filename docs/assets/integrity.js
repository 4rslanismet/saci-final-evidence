(()=>{"use strict";const en=(document.documentElement.lang||"").toLowerCase().startsWith("en"),root=en?"../":"",text=(tr,e)=>en?e:tr,esc=v=>String(v??"").replace(/[<>&"]/g,c=>({"<":"&lt;",">":"&gt;","&":"&amp;",'"':"&quot;"}[c]));async function load(p){const r=await fetch(root+p,{cache:"no-store"});if(!r.ok)throw new Error(`HTTP ${r.status}`);return r.json()}async function init(){try{const[s,f]=await Promise.all([load("data/final/integrity_summary.json"),load("data/final/integrity_findings.json")]);document.querySelector("#integrityScore").textContent=s.calculated_saci??"N/A";document.querySelector("#integrityClosure").textContent=`${s.observed_edges}/${s.edges}`;const st=document.querySelector("#integrityStatus");st.textContent=s.integrity_status;st.dataset.status=s.integrity_status;document.querySelector("#integrityGate").textContent=s.publication_gate;document.querySelector("#integrityCounts").textContent=text(`${s.finding_counts.INVALID} geçersiz · ${s.finding_counts.WARNING} uyarı`,`${s.finding_counts.INVALID} invalid · ${s.finding_counts.WARNING} warnings`);document.querySelector("#integrityRows").innerHTML=f.length?f.map(x=>`<tr><td><strong>${esc(x.finding_id)}</strong></td><td><span class="severity-chip ${esc(x.severity)}">${esc(x.severity)}</span></td><td><strong>${esc(en?x.title_en:x.title_tr)}</strong><br>${esc(en?x.detail_en:x.detail_tr)}</td><td>${esc(x.object_type)}<br><code>${esc(x.object_id)}</code></td><td>${esc(en?x.remediation_en:x.remediation_tr)}</td></tr>`).join(""):`<tr><td colspan="5">${text("Aktif bulgu yok.","No active finding.")}</td></tr>`;document.querySelector("#integrityLoading").hidden=true;document.querySelector("#integrityContent").hidden=false}catch(e){document.querySelector("#integrityLoading").textContent=text(`Integrity çıktıları yüklenemedi: ${e.message}`,`Integrity outputs could not be loaded: ${e.message}`)}}document.readyState==="loading"?document.addEventListener("DOMContentLoaded",init,{once:true}):init()})();