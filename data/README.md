# Data

The single canonical publication dataset is located at:

```text
docs/data/final/
```

Historical sensitivity scenarios are located at:

```text
docs/data/scenarios/
```

This pointer directory exists to avoid maintaining a second, divergent copy of the final evidence package.
